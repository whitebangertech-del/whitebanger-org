---
description: Deploy the complete website directly to Hostinger
---

This workflow will automatically synchronize your latest compiled code to Hostinger via FTP.
Wait! You MUST have your `.ftp-password` file manually created and containing your password before running this.

// turbo
1. Execute the deployment script:
   Command: `powershell -ExecutionPolicy Bypass -File deploy.ps1`
